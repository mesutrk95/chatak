<!DOCTYPE html>
<html lang="en"> 
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Chatak</title>
        <link rel="stylesheet" href="style.css" />
        <script src="/socket.io/socket.io.js"></script>
        <script src="https://kit.fontawesome.com/c939d0e917.js"></script>
        <script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    </head>

    <body>  
        <div class="d-flex mt-5">
            <div class="col users" style="margin-left: 30px;"> 
                <h3>Users (<%= users.length %>): </h3>
                <% users.forEach(user => { %>
                    <div class="user" style="margin-left: 30px;">
                        <b><%= user.username %></b> 
                        <span style="color: brown;">[<%= user.socketId %>]</span>
                    </div>
                <%}) %>
            </div> 
            <div class="col conns" style="margin-left: 30px;"> 
                <h3>Connections (<%= conns.length %>): </h3>
                <% conns.forEach(conn => { %>
                    <div class="conn" style="margin-left: 30px;">
                        <%= conn.u1 %> ->
                        <%= conn.u2 %>
                    </div>
                <%}) %>
            </div>  
        </div>
        <div class="d-flex mt-5"> 
            <div class="col searching-users" style="margin-left: 30px;display: block;"> 
                <h3>Searching Users (<%= searchingUsers.length %>): </h3>
                <% searchingUsers.forEach(su => { %>
                    <div class="conn" style="margin-left: 30px;">
                        <%= su.username %> 
                        <span style="color: brown;">[<%= su.socketId %>]</span> 
                    </div>
                <%}) %>
            </div>  
        </div>
    </body>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script> 

</html>